import { describe, it, expect } from 'vitest';
import EggHatchSceneHandler from './egg-hatch-scene-handler';

describe('EggHatchSceneHandler', () => {
  it('should exist', () => {
    expect(EggHatchSceneHandler).toBeDefined();
  });
});