import { describe, it, expect } from "vitest";
import BattleScene from "./battle-scene";

describe("BattleScene", () => {
  it("should exist", () => {
    expect(BattleScene).toBeDefined();
  });
});
